# Helm values reference

**Audience:** Platform operator

!!! note
    This page is auto-generated from `helm/values.yml` via
    [helm-docs](https://github.com/norwoodj/helm-docs). Do not edit it
    directly — annotate the values file with `# --` comments and re-run
    `make helm-docs` (or rely on the pre-commit hook). The
    `helm-docs-check` CI gate fails the build if the regenerated content
    drifts from what is committed.

---

--8<-- "helm/README.md"
